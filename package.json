{
  "private": true,
  "scripts": {
    "dev:server": "npm run dev --prefix server",
    "dev:app": "npm run dev --prefix app",
    "dev": "concurrently -n server,app -c green,cyan \"npm:dev:server\" \"npm:dev:app\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
